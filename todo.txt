ADDITIONS:
    1. Try catch wrapping were possible errors might occur and dispatch error message to user when catched.
    2. Multi-line user commands
    3. Test scripts for specific individual components
    4. Add storage of schedule when the application is stopped and retrieval of the stored schedule when it is started.
    5. Testing without whatsapp. A script that automates dummy requests to api disguised as Twilio requests.
    6. Add minimum time gap check between consecutive task objects in TaskQueue (during insertion).
